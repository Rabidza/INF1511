# import calendar
#
# year = int(input("Type in the year number: "))
# calendar.prcal(year)

# above can also be rewritten as:
from calendar import prcal
year = int(input("Type in the year number: "))
prcal(year)
